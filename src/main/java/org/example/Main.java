package org.example;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class Main {
    public static void main(String[] args) throws InterruptedException {
//        WebDriver driver = new ChromeDriver() ;
//        driver.get("https://teams.microsoft.com/");
//        System.out.println("Title: " + driver.getTitle());
//
//        // Wait for a few seconds to ensure the page is loaded
//        Thread.sleep(5000);
//
//        // Print all the fields on the page
//        System.out.println("Printing all fields on the page:");
//        java.util.List<WebElement> allFields = driver.findElements(By.tagName("input"));
//        String userName = "talsahar11@gmail.com" ;
//        for (WebElement field : allFields) {
//            System.out.println("Field Name: " + field.getAttribute("name") + ", Field Value: " + field.getAttribute("value") +
//                    ", Type: " + field.getAttribute("type") + ", ID: " + field.getAttribute("id"));
//            if (field.getAttribute("name").equals("loginfmt")) {
//
//            }
//        }
        Simulator simulator = new Simulator() ;
    }
}